/* enum Direction {
  UP,
  DOWN,
  LEFT,
  RIGHT,
}

const dir = Direction.DOWN
console.log(dir) // 값이 1, 상수가 정의된 순서 번호(참고 - 자바의 ordinal 값과 유사)

let value = 'DOWN'
const dir2 = value as any as Direction
// console.log(value === dir)
console.log(dir2 == dir) */